{{ 'logo-marquee.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.mobile_padding_top}}px;
    padding-bottom: {{ section.settings.mobile_padding_bottom }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}
<div
  class="logo-marquee gradient color-{{ section.settings.section_color_scheme }} section-{{ section.id }} section-{{ section.id }}-padding {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
  data-logo-marquee
  data-section-id="{{ section.id }}"
  data-speed="{{ section.settings.marquee_speed }}"
  data-direction="{{ section.settings.direction }}"
  data-pause-hover="{{ section.settings.pause_on_hover }}"
  data-mobile-enable="{{ section.settings.mobile_enable }}"
  style="
    --logo-height: {{ section.settings.logo_height }}px;
    --logo-width: {{ section.settings.logo_width }}px;
    --logo-gap: {{ section.settings.logo_gap }}px;
  "
>
  <div class="logo-marquee__wrapper">
    <div
      class="logo-marquee__viewport"
      data-marquee-viewport
    >
      <div
        class="logo-marquee__track"
        data-marquee-track
      >
        {% for block in section.blocks %}
          {% assign logo_image = block.settings.logo %}

          <div
            class="logo-marquee__item"
            {{ block.shopify_attributes }}
          >
            {% if block.settings.link != blank %}
              <a
                href="{{ block.settings.link }}"
                class="logo-marquee__link"
                {% if block.settings.open_new_tab %}
                  target="_blank"
                  rel="noopener noreferrer"
                {% endif %}
              >
            {% endif %}
               
                    {% render 'smart-image',
                      image: block.settings.logo_image,
                      width: 110,
                      widths: '70,90,100,110',
                      sizes: '110'
                    %}

            {% if block.settings.link != blank %}
              </a>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    </div>

  </div>
</div>

<script src="{{ 'logo-marquee.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Logo Marquee",
  "tag": "section",
  "class": "section-logo-marquee",
  "settings": [

          {
              "type": "color_scheme",
              "id": "section_color_scheme",
              "label": "Scheme"
            },
    {
      "type": "range",
      "id": "logo_height",
      "label": "Logo height",
      "min": 20,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 42
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Logo area width",
      "min": 100,
      "max": 300,
      "step": 5,
      "unit": "px",
      "default": 170
    },
    {
      "type": "range",
      "id": "logo_gap",
      "label": "Logo gap",
      "min": 4,
      "max": 30,
      "step": 2,
      "unit": "px",
      "default": 4
    },
    {
      "type": "range",
      "id": "marquee_speed",
      "label": "Speed",
      "min": 10,
      "max": 100,
      "step": 5,
      "unit": "s",
      "default": 35
    },
    {
      "type": "select",
      "id": "direction",
      "label": "Direction",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "checkbox",
      "id": "pause_on_hover",
      "label": "Pause on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "mobile_enable",
      "label": "Enable marquee on mobile",
      "default": true
    },
    
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "paragraph",
      "content": "Desktop"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
    {
      "type": "paragraph",
      "content": "Mobile"
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Bottom",
      "default": 36
    }


  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo image"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Logo link"
        },
        {
          "type": "checkbox",
          "id": "open_new_tab",
          "label": "Open link in new tab",
          "default": false
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Logo Marquee",
      "blocks": [
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        },
        {
          "type": "logo"
        }
      ]
    }
  ]
}
{% endschema %}